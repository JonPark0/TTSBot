Cloudflare의 AI 서비스를 활용하여 Discord 서버에서 사용할 TTS 봇을 개발하려고 해.

다음과 같은 구조로 개발을 해 줘.

Docker Compose V2 이용
.env를 활용해서 환경 변수를 설정할 수 있게 할 것. .env.example을 구성할 것.

Cloudflare AI Workers의 경우 무료 한도 내에서 사용할 수 있도록 할 것. 어떤 모델을 사용할 것인지 모델 ID를 환경 변수로 지정 가능하게 할 것.

일단은 한국어를 지원하면서 가장 저렴한 `@cf/myshell-ai/melotts` 모델을 사용하려고 함.

Discord 서버의 관리자가 특정 채널을 TTS 채널로 지정하면 해당 채널에 올라오는 메시지들을 TTS로 해서 해당 메시지를 작성한 사람이 속해 있는 음성 채널에 음성으로 출력해 줄 것.

이모티콘, 스티커, gif, 이미지, URL, 특수문자 및 유니코드 등의 불필요한 요소들은 Sanitization 할 수 있도록 할 것.

텍스트 내용이 한국어/영어/중국어/일본어인지 구분해서 TTS를 진행할 수 있게 할 것.